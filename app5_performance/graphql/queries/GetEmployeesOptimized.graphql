query GetEmployeesOptimized($organizationId: Int!) {
  employeesByOrganization(organizationId: $organizationId) {
    ...EmployeeFields
    organization {
      ...OrganizationFields
    }
  }
}
